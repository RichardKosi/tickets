#include <stdlib.h>
#include <string>
#include <iostream>
#include <limits.h>

using std::cout;
using std::cin;
using std::endl;

int main()
{
	int lenstr = 256;
	char *str = new char[lenstr];
	char *bin = new char[lenstr];
	cin >> str;
	int i = 0;
	for (; i < lenstr && str[i] != '\0'; i++)

	return 0;
}
